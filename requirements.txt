# Runtime — AI Agent 7h Bot + Formal Engine + Spec Translator
python-telegram-bot>=21.0,<22
python-dotenv>=1.0.0
pydantic>=2.5,<3
pydantic-settings>=2.1,<3

# Spec Translator uses the Hugging Face OpenAI-compatible Router (preferred).
requests>=2.31.0

curl_cffi>=0.6.0
aiohttp>=3.9.0

# Optional only — g4f is OFF by default (G4F_ENABLED=0). Prefer HF_TOKEN.
# Uncomment only if you explicitly need the fallback:
# g4f>=0.3.0
