# Web control plane

Next.js UI under `web/` for job runs, agent views, and diffs.

Talks to the B2B API (`/v1/jobs/*`, runs_ux). See root `docs/API.md`.

```bash
cd web && npm install && npm run dev
```
