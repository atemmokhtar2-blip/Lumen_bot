# Required — get it from @BotFather
TELEGRAM_BOT_TOKEN=123456:ABC-DEF...

# REQUIRED for SpecTranslator (Hugging Face only — no structural fallback)
# Get a token: https://huggingface.co/settings/tokens
HF_TOKEN=hf_your_token_here
HF_ENABLED=1
# HF_MODELS=Qwen/Qwen2.5-72B-Instruct,meta-llama/Llama-3.3-70B-Instruct,meta-llama/Llama-3.1-8B-Instruct

# Spec Translator timeout (seconds)
SPEC_TRANSLATOR=1
SPEC_TRANSLATOR_TIMEOUT=45

# Optional — comma-separated Telegram user IDs
# ALLOWED_USER_IDS=123456789

# Optional — where generated projects are stored
# OUTPUT_DIR=/tmp/generated

# Live run duration for generated/cloned bots (seconds)
LIVE_RUN_SECONDS=900
