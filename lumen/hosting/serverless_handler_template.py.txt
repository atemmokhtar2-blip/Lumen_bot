# @@MARKER@@
# Generated by Lumen for serverless Telegram webhook hosting.
# Do not put BOT_TOKEN in this file — runtime injects env only.
from __future__ import annotations

import json
import logging
import os
import sys
from http.server import BaseHTTPRequestHandler
from pathlib import Path

logging.basicConfig(level=logging.INFO)
log = logging.getLogger("lumen.serverless.webhook")

_ROOT = Path(__file__).resolve().parent.parent
if str(_ROOT) not in sys.path:
    sys.path.insert(0, str(_ROOT))

_FRAMEWORK = "@@FRAMEWORK@@"
_ENTRY = "@@ENTRY@@"


def _token() -> str:
    return (os.environ.get("BOT_TOKEN") or os.environ.get("TELEGRAM_BOT_TOKEN") or "").strip()


def _load_ptb_application():
    import importlib
    mod = importlib.import_module(_ENTRY)
    for name in ("application", "app", "APPLICATION", "APP"):
        app = getattr(mod, name, None)
        if app is not None and hasattr(app, "process_update"):
            return app
    for name in ("build_application", "create_application", "make_application", "get_application"):
        fn = getattr(mod, name, None)
        if callable(fn):
            app = fn()
            if app is not None and hasattr(app, "process_update"):
                return app
    return None


def _process_ptb(update_data: dict) -> None:
    from telegram import Update
    app = _load_ptb_application()
    if app is None:
        raise RuntimeError("ptb_application_not_found")
    bot = getattr(app, "bot", None)
    update = Update.de_json(update_data, bot)
    result = app.process_update(update)
    if hasattr(result, "__await__"):
        import asyncio
        asyncio.get_event_loop().run_until_complete(result)


def _process_telebot(update_data: dict) -> None:
    import importlib
    mod = importlib.import_module(_ENTRY)
    bot = None
    for name in ("bot", "BOT", "tele_bot"):
        bot = getattr(mod, name, None)
        if bot is not None and hasattr(bot, "process_new_updates"):
            break
    if bot is None:
        raise RuntimeError("telebot_instance_not_found")
    from telebot.types import Update
    upd = Update.de_json(update_data)
    bot.process_new_updates([upd])


def _process_aiogram(update_data: dict) -> None:
    import asyncio
    import importlib
    from aiogram.types import Update

    mod = importlib.import_module(_ENTRY)
    dp = None
    bot = None
    for name in ("dp", "dispatcher", "Dispatcher"):
        dp = getattr(mod, name, None)
        if dp is not None:
            break
    for name in ("bot", "Bot", "BOT"):
        b = getattr(mod, name, None)
        if b is not None:
            bot = b
            break
    if dp is None:
        raise RuntimeError("aiogram_dispatcher_not_found")
    token = _token()
    if bot is None and token:
        from aiogram import Bot
        bot = Bot(token=token)
    update = Update(**update_data)

    async def _run():
        if hasattr(dp, "feed_update"):
            await dp.feed_update(bot, update)
        elif hasattr(dp, "process_update"):
            await dp.process_update(update)
        else:
            raise RuntimeError("aiogram_no_feed")

    asyncio.get_event_loop().run_until_complete(_run())


def process_telegram_update(update_data: dict) -> None:
    if not _token():
        raise RuntimeError("bot_token_missing_in_env")
    if _FRAMEWORK == "python-telegram-bot":
        _process_ptb(update_data)
    elif _FRAMEWORK == "telebot":
        _process_telebot(update_data)
    elif _FRAMEWORK == "aiogram":
        _process_aiogram(update_data)
    else:
        try:
            _process_ptb(update_data)
            return
        except Exception:
            pass
        _process_telebot(update_data)


class handler(BaseHTTPRequestHandler):
    def log_message(self, fmt, *args):
        log.info(fmt, *args)

    def _send(self, code: int, body: dict):
        raw = json.dumps(body).encode("utf-8")
        self.send_response(code)
        self.send_header("Content-Type", "application/json")
        self.send_header("Content-Length", str(len(raw)))
        self.end_headers()
        self.wfile.write(raw)

    def do_GET(self):
        self._send(200, {"ok": True, "service": "lumen-bot", "framework": _FRAMEWORK})

    def do_POST(self):
        try:
            length = int(self.headers.get("Content-Length") or 0)
        except Exception:
            length = 0
        if length <= 0 or length > 2_000_000:
            self._send(400, {"ok": False, "error": "invalid_body"})
            return
        raw = self.rfile.read(length)
        try:
            data = json.loads(raw.decode("utf-8"))
        except Exception:
            self._send(400, {"ok": False, "error": "invalid_json"})
            return
        if not isinstance(data, dict):
            self._send(400, {"ok": False, "error": "invalid_update"})
            return
        try:
            process_telegram_update(data)
        except Exception as exc:
            log.exception("update_failed")
            self._send(500, {"ok": False, "error": type(exc).__name__})
            return
        self._send(200, {"ok": True})
