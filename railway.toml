[build]
builder = "DOCKERFILE"
# Dockerfile pins Python 3.10 and installs both runtime and dialogue dependencies.

[deploy]
startCommand = "python main.py"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
