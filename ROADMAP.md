# AI Agent 7h Bot — التوثيق الوحيد

> هذا هو **ملف التوثيق الوحيد** في المشروع.
> أي إنجاز: **تم** | جاري | قادم

---

## ⛔ قواعد حاسمة — غير قابلة للتفاوض

### 1) مستحيل دخول أي ذكاء اصطناعي (LLM / AI models)

- مسار الفهم والتوليد والبوابات **حتمي 100%**.
- **لا** استدعاء لـ OpenAI / Gemini / Claude / أي نموذج لغوي في:
  - Understanding
  - Planning
  - Codegen
  - StaticDevGate / FinalGate
  - Repo understanding / dev
- الأدوات المسموحة فقط: `ast`، قواعد، أنطولوجيا، عقود بيانات، تحليل تدفق، تنفيذ رمزي.
- أي مكتبة AI في مستودع **مستخدم** تُرصد كقدرة خارجية — **لا تُحقن** في محرك التوليد نفسه.
- رسالة المحرك الرسمية:  
  **«Formal Engine — بدون نماذج لغوية.»**

### 2) مستحيل على المطوّر استخدام قوالب جاهزة

- **ممنوع** قوالب دومين (متجر / توصيل / تذاكر / ألعاب…) تُسقط أوامر أو handlers جاهزة.
- **ممنوع** `default_commands` / `default_buttons` / `command_from_entity`.
- الأوامر = ما كتبه المستخدم في النص فقط.
- الكيانات والحقول = إشارات من النص + حد أدنى بنيوي — **ليس** تفريغ مكتبة كيانات كاملة.
- الكود يُبنى من `ProgramContract` فقط (بنيوي):
  - handlers من قائمة الأوامر
  - services من الكيانات/التقنية
  - FSM من `conversation_states` + حقول الكيان المذكورة في النص
  - إطار العمل (aiogram / PTB) من النص/العقد
- تم حذف مسار التوليد القديم (`templates.py` / `dynamic_codegen` / `project_generator`).

### 3) المكتبة والمحرك

- المحرك الرسمي: `telegram_bot_engine.formal_engine`
- المدخل: `generate_bot` / `run_formal_pipeline`
- المسار الوحيد:

```
نص المستخدم
  → UnderstandingService  (استخراج عقد من النص)
  → PlanningService       (إكمال بنيوي بدون أوامر مخترعة)
  → CodegenService        (ملفات من العقد فقط)
  → StaticDevGate         (أنماط + تدفق + رمزي + عقود)
  → FinalGate             (fidelity + conversation + static)
  → py_compile
```

---

## الهدف

نظام يفهم طلب بشري **حرفياً بدون LLM**، يولّد مشروعاً حتمياً، يمرّ على بوابات ثابتة، خلال زمن قصير.

أمثلة طلبات:
- «اعمل بوت فيه /start /help /track وطلبات فيها عنوان وهاتف»
- «اسحب المستودع ده وطوّر عليه»
- «صلّح الأمر X في المشروع القائم»

---

## مسار التوليد (الواقع الحالي)

| مرحلة | الوظيفة | حالة |
|--------|---------|------|
| Understanding | أوامر/أزرار/كيانات/حقول/إطار من النص | تم |
| Planning | services من كيانات — **بدون** اختراع أوامر | تم |
| Codegen | aiogram/PTB + DI + repos (Postgres أو memory) | تم |
| FSM | حقول الكيان من النص (مثل address/phone) | تم |
| أزرار | الأول يبدأ flow، الباقي list على الكيان الأساسي | تم |
| StaticDevGate | patterns / dataflow / symbolic / contracts | تم |
| FinalGate | fidelity + conversation_flow + static | تم |
| حذف القوالب | `templates.py` + `dynamic_codegen` + `project_generator` | تم |

---

## ما الذي لا يحدث أبداً

| ممنوع | السبب |
|--------|--------|
| استدعاء LLM أثناء التوليد | يكسر الحتمية وقابلية التحقق |
| قالب «بوت توصيل جاهز» | يخالف النص الحرفي للمستخدم |
| أوامر `/order` `/user` من اسم كيان | الأوامر من النص فقط |
| تفريغ كل حقول `ENTITY_LIBRARY` | الحقول بإشارة نصية فقط |
| fallback لمولّد قوالب قديم | المسار محذوف |

---

## محركات Git / Repo (بدون LLM أيضاً)

- سحب مستودع، مسح AST، `RepoContract`
- تطوير أوامر على مشروع قائم بقواعد ثابتة
- لا نماذج لغوية في هذه الطبقة

---

## للمطوّر الذي يلمس الكود

1. لا تُضف استيراد `openai` / `anthropic` / SDK نماذج داخل `formal_engine/`.
2. لا تُعد مسارات `render_*_handler` حسب نوع بوت.
3. أي أمر جديد = يظهر في النص → العقد → handler.
4. أي كيان جديد = إشارة في النص → `resolve_data_models` → حقول بإشارة.
5. بعد أي تعديل على المحرك: اختبار `generate_bot` محلياً + push.

---

## المراحل المتبقية (مختصر)

| # | المهمة | حالة |
|---|--------|------|
| تشغيل البوت المولَّد واختبار أوامر حية | قادم |
| schema SQL ثابت من حقول الكيان بدل JSONB العام | قادم |
| توسيع الأنطولوجيا النصية (بدون packs أوامر) | قادم |
| اختبارات ضغط أطول على أوصاف متعددة اللغات | قادم |

---

**الخلاصة سطر واحد:**  
هذا المحرك **حتمي، بدون ذكاء اصطناعي، وبدون قوالب جاهزة** — من النص إلى العقد إلى الكود إلى البوابة فقط.
