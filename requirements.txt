# Runtime — AI Agent 7h Bot + Formal Engine + Spec Translator
python-telegram-bot>=21.0,<22
python-dotenv>=1.0.0
pydantic>=2.5,<3
pydantic-settings>=2.1,<3

# Spec Translator (AI speech → formal JSON only). Never imported inside formal_engine.
# Installed on Railway/hosting so g4f is available at runtime.
g4f>=0.4.0.0
curl_cffi>=0.6.0
aiohttp>=3.9.0
