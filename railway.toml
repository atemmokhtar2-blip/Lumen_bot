[build]
builder = "DOCKERFILE"
# Dockerfile (python:3.12-slim-bookworm) installs requirements.txt and runs main.py.

[deploy]
startCommand = "python main.py"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
