# Runtime — Lumen (exact pins; lockfile is requirements.lock)
# Bump only after: pip-audit + pip-compile
python-telegram-bot==21.10
python-dotenv==1.2.2
pydantic==2.10.6
pydantic-settings==2.7.1
requests==2.33.0
curl_cffi==0.15.0
aiohttp==3.14.3

# Optional: faster fuzzy matching for Layer-1 LU
rapidfuzz==3.12.1
pymongo==4.11.1

# Security / hosting foundation
cryptography==50.0.0
# Optional object storage (S3/R2/MinIO)
# boto3==1.36.16
# Optional Postgres host state
psycopg[binary]==3.2.4

redis==5.2.1
rq==2.1.0

# Observability (OpenTelemetry + Prometheus)
opentelemetry-api==1.33.1
opentelemetry-sdk==1.33.1
opentelemetry-exporter-otlp-proto-http==1.33.1
prometheus-client==0.26.0

# Database migrations (Alembic + SQLAlchemy engine for migration runner only)
SQLAlchemy==2.0.40
alembic==1.15.2

# Phase B — durable workflows (official SDK)
temporalio>=1.7.0,<2
