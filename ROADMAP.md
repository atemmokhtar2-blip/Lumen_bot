# خطة المشروع طويلة المدى — AI Agent 7h Bot

> هذا هو **ملف التوثيق الوحيد** في المشروع.
> أي إنجاز يُعلَّم عليه: **تم**
> أي عمل جاري: **جاري**
> أي مؤجل: **قادم**

---

## الهدف النهائي (المرحلة المرعبة)

نظام يفهم أي طلب بشري حرفياً (بدون LLM)، ينفّذ بجودة قصوى، خلال ثوانٍ، بدون أخطاء، ثم يشغّل الناتج ويختبر أن كل الأوامر تعمل فعلياً.

أمثلة على ما يجب أن ينجح:
- «اعمل بوت متجر + دفع + إدارة»
- «اسحب المستودع ده وطوّره يضيف أمر X»
- «صلّح البوت اللي شغال وأضف نظام تذاكر»
- أي مهمة برمجية/تشغيلية ضمن نطاق البوتات والأنظمة المرتبطة

المعايير النهائية:
1. فهم كامل ودقيق للطلب والسياق (نص / مستودع مسحوب / مشروع قائم)
2. تخطيط ثم توليد/تعديل كود حتمي عالي الجودة
3. تشغيل البوت تلقائياً
4. اختبار الأوامر والتأكد أنها شغالة
5. زمن استجابة منخفض (هدف: ثوانٍ معدودة)
6. صفر أخطاء تشغيلية في المسار السعيد

---

## القاعدة الصارمة للجودة

أي أساس أو محرك أو فهم يُبنى هنا يجب أن يكون:
- متيناً (عقود صارمة + اختبار)
- بأفضل أدوات مناسبة للمهمة (مثل `ast` للبايثون وليس regex فقط)
- أعلى جودة عملية ممكنة بدون LLM في المسار الحتمي
- قابلاً للتحقق والتوسع

---

## المبادئ الثابتة

- لا اعتماد على نماذج لغوية (LLM) في مسار الفهم/التوليد الأساسي.
- الفهم = قواعد + أنطولوجيا + عقود بيانات صارمة + محركات متخصصة.
- العقد المركزي بين الفهم والتوليد يبقى قوياً (`ProgramContract` أو ما يتطور عنه).
- محركات Git/Repo تبقى (سحب، فهم مستودع، تطوير عليه).
- نطوّر **حاجة واحدة في كل مرة**، ونعلّمها هنا **تم**.

---

## المراحل

### المرحلة 0 — الأساس الحالي
| # | المهمة | الحالة |
|---|--------|--------|
| 0.1 | مسار Formal: Understanding → Codegen | تم |
| 0.2 | PlanningService قبل التوليد (اكتمال الدومين + tech + readiness) | تم |
| 0.3 | Hybrid: الإبقاء على محركات التخطيط/المراجعات/Git وحذف الزائد | تم |
| 0.4 | إصلاح أخطاء النشر (SyntaxError في main.py) | تم |
| 0.5 | ذكاء الشات: تمييز «اسحب المستودع» عن «ولّد بوت» + smart_clone | تم |
| 0.6 | ملف توثيق واحد = هذه الخطة | تم |

### المرحلة 1 — فهم ما بعد السحب (Repo Intelligence)
> الهدف: بعد `clone`، النظام يقرأ المستودع ويبني نموذجاً داخلياً يصلح للتطوير عليه.

| # | المهمة | الحالة |
|---|--------|--------|
| 1.1 | بعد السحب: فهم تلقائي للمستودع + حفظ سياق المستخدم | تم |
| 1.2 | محرك مسح المستودع العميق (AST + طبقات + أوامر مسجّلة + نماذج + محركات) | تم |
| 1.3 | بناء `RepoContract` (مشابه لـ ProgramContract لكن لمشروع موجود) | تم |
| 1.4 | ربط الشات: تطوير على المستودع النشط (شرح/أوامر/ملفات/بحث/إضافة/حذف أمر + رفض صادق) | تم |
| 1.5 | تخزين سياق المستودع لكل مستخدم (آخر clone + RepoContract) | تم |
| 1.6 | طبقة ذكاء المستودع (RepoIntelligence): فجوات تبعيات + جاهزية استضافة + قدرات + مخاطر + خطوات تالية | تم |
| 1.7 | فهرس كود عميق في نفس الـ AST pass: كل دالة/منهج + استدعاءات + CodeGraph خلال <2ث | تم |

### المرحلة 2 — فهم عميق للنص (Understanding Evolution)
> الهدف: المحرك الأساسي يفهم الطلب حرفياً بدقة أعلى بدون LLM.

| # | المهمة | الحالة |
|---|--------|--------|
| 2.1 | توسيع الأنطولوجيا (أنواع بوتات، كيانات، تدفقات، تكاملات) | قادم |
| 2.2 | فهم الأقسام الطويلة والمنظمة (مواصفات 3000+ حرف) بدقة أعلى | قادم |
| 2.3 | كشف النواقص والتعارضات وطلب توضيح منظم عند الحاجة | قادم |
| 2.4 | توحيد المخرجات دائماً على عقد صارم قابل للتحقق | قادم |
| 2.5 | اختبارات فهم: مئات الطلبات العربية/الإنجليزية بحالات حدية | قادم |

### المرحلة 3 — تخطيط ومراجعات مربوطة بالعقد
> الهدف: محركات التخطيط والمراجعات تشتغل فعلياً على العقد/الملفات لا على artefacts قديمة ميتة.

| # | المهمة | الحالة |
|---|--------|--------|
| 3.1 | تكييف Risk / Architecture / Technology لتعمل على ProgramContract | قادم |
| 3.2 | طبقة Review بعد التوليد: Static Analysis + Security على الملفات | قادم |
| 3.3 | بوابة جودة: لا يُسلَّم مشروع إن فشلت المراجعة الحرجة | قادم |
| 3.4 | تحسين Codegen ليطيع نتائج التخطيط (مش قالب أعمى فقط) | قادم |

### المرحلة 4 — تشغيل واختبار حي (Live Verify)
> الهدف: بعد التوليد، البوت يشتغل والأوامر تُختبر.

| # | المهمة | الحالة |
|---|--------|--------|
| 4.1 | تشغيل المشروع في بيئة معزولة (venv أو pip --target + token) | تم |
| 4.2 | اختبار الأوامر الأساسية (/start /help والأوامر المولَّدة) | قادم |
| 4.3 | تقرير صحة: أخطاء التشغيل + لوج + getMe | تم (أساس) |
| 4.4 | ربط LiveDeployment الحالي ببوابة الجودة السابقة | قادم |
| 4.5 | Auto-Heal: ModuleNotFoundError → إضافة الحزمة لـ requirements + إعادة تثبيت وتشغيل (حتى 2 جولات) | تم |
| 4.6 | محرك أخطاء (Error Intelligence): تحليل لوج → ErrorContract + تصنيف + إجراء مقترح | تم |
| 4.7 | ربط Error Intelligence كمصدر قرار وحيد لـ Auto-Heal | تم |
| 4.8 | أساس الاستضافة (HostingService): start/stop/status/diagnose بدون دفع | تم |

### المرحلة 5 — تطوير تكراري على مشروع قائم
> الهدف: المستخدم يقول «عدّل / أضف / صلّح» والنظام يعدّل الكود بفهم.

| # | المهمة | الحالة |
|---|--------|--------|
| 5.0 | ذكاء تطوير بعد الفهم (DevPlan من CodeGraph + سد فجوات + أهداف تعديل) | تم |
| 5.0b | ActiveDevEngine: مواصفات طويلة → FormalBotSpec → فرق أوامر/تبعيات → تنفيذ AST + تقرير صادق | تم |
| 5.1 | أوامر شات: أضف أمر، عدّل زر، صلّح خطأ، أضف كيان | قادم |
| 5.2 | تطبيق تعديلات على الملفات انطلاقاً من RepoContract + طلب جديد | قادم |
| 5.3 | إعادة verify + تشغيل + اختبار بعد كل تعديل | قادم |

### المرحلة 6 — السرعة والجودة القصوى
| # | المهمة | الحالة |
|---|--------|--------|
| 6.1 | قياس زمن كل مرحلة وتحسين الاختناقات | قادم |
| 6.2 | كاش للأنطولوجيا والعقود المتكررة | قادم |
| 6.3 | هدف زمني: مسار سعيد كامل في ثوانٍ معدودة | قادم |
| 6.4 | صفر أخطاء في مجموعة اختبار ذهبية ثابتة | قادم |

### المرحلة 7 — التوسع (أي مهمة تقريباً)
| # | المهمة | الحالة |
|---|--------|--------|
| 7.1 | توسيع نطاق ما يمكن توليده (أنواع أنظمة أوسع ضمن حدود حتمية) | قادم |
| 7.2 | سياسات أمان وحدود لما يُرفض تنفيذه | قادم |
| 7.3 | تجربة مستخدم نهائية: من الجملة → مشروع شغّال مختبَر | قادم |

---

## أول نقص حقيقي الآن (نقطة البداية التالية)

**تم إنجاز 1.1 / 1.2 / 1.3 / 1.5** (مسح + RepoContract + فهم تلقائي بعد السحب + سياق مستخدم).

**التالي: مرحلة 2 / توسيع التعديل** — تعديلات أعمق من إضافة أمر + جودة أعلى  
(مثال: «أضف أمر /stats» يشتغل على `active_repo` بدل ما يتجاهله).

ثم المرحلة 2: تعميق محرك الفهم النصي.

---

## سجل الإنجاز السريع

- تم: مسار Formal + Planning
- تم: تنظيف المحركات مع الإبقاء على Git
- تم: إصلاح نشر Railway
- تم: تمييز سحب المستودع عن التوليد
- تم: هذه الخطة كملف التوثيق الوحيد
- تم: فهم المستودع بعد السحب (RepoContract + scanner + سياق)
- تم: RepoIntelligence v1 فوق RepoContract — host_readiness، dependency_gaps، capabilities، risks، next_actions، change_surface
- تم: ترقية الفهم إلى RepoContract v2 (AST عميق + معمارية + جودة)
- تم: scanner v3 — دعم aiogram/telebot/PTB + تمييز مكتبة vs بوت + README
- تم: أوامر تطوير على المستودع النشط (1.4) — شرح/أوامر/إضافة أمر PTB+aiogram+telebot
- تم: RepoDev v2 — عرض ملف، بحث، حذف أمر، قائمة ملفات، تحقق syntax قبل الكتابة
- تم: LiveRunner — getMe + تثبيت تبعيات حقيقي + تشغيل عملية + محرك أخطاء
- تم: سحب مستودع خاص → طلب GitHub PAT وإعادة السحب
- تم: تسريع clone (depth/filter) + تقليل مسح AST
- تم: بعد السحب يطلب التوكن للتشغيل الحقيقي
- تم: LiveRunner Auto-Heal — اكتشاف ModuleNotFoundError، تعيين الحزمة، إضافتها لـ requirements، إعادة تثبيت وتشغيل (مختبَر orjson E2E)
- قادم: اختبار أوامر حي عبر Telegram API + تعديلات أعمق + إصلاح أخطاء runtime أوسع

---

## قواعد العمل من الآن

1. ننفّذ بنداً واحداً من الجدول في كل دورة تطوير.
2. بعد الإنجاز: نحدّث صفّه إلى **تم** في هذا الملف.
3. لا نضيف ملفات توثيق أخرى — كل شيء هنا.
4. لا نكسر محركات Git.
5. أي فهم جديد يجب أن يصب في عقد بيانات قابل للتحقق، لا في نص حر فقط.
