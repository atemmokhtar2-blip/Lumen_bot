[build]
builder = "NIXPACKS"
# Python 3.10 required for rasa==3.6 (no wheels on 3.12)
buildCommand = "pip install -r requirements.txt && pip install -r requirements-dialogue.txt"

[deploy]
startCommand = "python main.py"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
