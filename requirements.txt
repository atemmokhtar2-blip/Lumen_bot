# Runtime — Lumen (exact pins; lockfile is requirements.lock)
# Bump only after: pip-audit + pip-compile
python-telegram-bot==21.10
python-dotenv==1.2.3
pydantic==2.10.6
pydantic-settings==2.7.1
requests==2.33.0
curl_cffi==0.15.0
aiohttp==3.14.3

# Optional: faster fuzzy matching for Layer-1 LU
rapidfuzz==3.12.1
pymongo==4.11.1

# Security / hosting foundation
cryptography==50.0.0
# Optional object storage (S3/R2/MinIO)
# boto3==1.36.16
# Optional Postgres host state
psycopg[binary]==3.2.4

redis==5.2.1
rq==2.1.0

# Observability (OpenTelemetry + Prometheus)
opentelemetry-api==1.33.1
opentelemetry-sdk==1.33.1
opentelemetry-exporter-otlp-proto-http==1.33.1
prometheus-client==0.21.1

# Database migrations (Alembic + SQLAlchemy engine for migration runner only)
SQLAlchemy==2.0.52
alembic==1.15.2

# Phase B — durable workflows (official SDK)
temporalio[langgraph]==1.32.0

# Phase C — Codebase Intelligence
tree-sitter>=0.22.0
tree-sitter-python>=0.23.0
rank-bm25>=0.2.2
jedi>=0.19.0
fastembed>=0.4.0
numpy>=1.24
tree-sitter-javascript>=0.23.0
qdrant-client>=1.9.0

# Computer use / browser agent (real Playwright)
playwright>=1.40.0,<2

# Optional tree-sitter languages (code intelligence multi-lang)
tree-sitter-typescript>=0.23.0
tree-sitter-go>=0.23.0
tree-sitter-java>=0.23.0
tree-sitter-rust>=0.23.0
tree-sitter-c>=0.23.0
tree-sitter-cpp>=0.23.0

# Official agent orchestration (LangGraph)
langgraph==1.2.11
langchain-core==1.6.1

# Official MCP Python SDK
mcp>=1.0.0,<2
