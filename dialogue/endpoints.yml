# Action server — optional in Phase 0 (utterances work without it).
# When custom actions are needed:
#   rasa run actions --actions actions -p 5055
action_endpoint:
  url: "http://localhost:5055/webhook"

# Tracker: in-memory by default. Swap to Mongo/Redis later without changing domain.
# tracker_store:
#   type: mongod
#   url: ${MONGODB_URI}
#   db: ai_agent_7h
#   collection: rasa_trackers
