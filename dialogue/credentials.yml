# Telegram is handled by bot_interface (python-telegram-bot).
# Rasa channel credentials stay empty in Phase 0 — dialogue_bridge owns the channel.
# telegram:
#   access_token: ${TELEGRAM_BOT_TOKEN}
#   verify: ${TELEGRAM_BOT_TOKEN}
rest:
