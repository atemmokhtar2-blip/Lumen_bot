# Archived legacy analysis modules

These historical analysis modules are retained for reference only. They are not imported by production code and are intentionally outside the default `tests/` collection path.

Do not add runtime imports from this directory. New behavior belongs under `telegram_bot_engine/` and must have focused tests under `tests/`.
