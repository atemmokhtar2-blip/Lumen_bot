# خطة تطوير AI Agent 7h Bot لفهم البوتات المعقدة والنصوص الطويلة

تستهدف هذه الخطة تحسين قدرة المحرك على استيعاب متطلبات برمجية معقدة ونصوص طويلة جداً مع الحفاظ على مبدأ "الحتمية" وعدم استخدام الذكاء الاصطناعي في توليد الكود.

## 1. تحسين فهم البوتات المعقدة (Complex Global Bots)

### أ. توسيع الأنطولوجيا (Ontology Expansion)
إضافة مفاهيم جديدة لـ `telegram_taxonomy.py` تدعم:
- **التكاملات الخارجية**: بوابة دفع (Stripe/MyFatoorah)، ربط API خارجي، Webhooks.
- **إدارة البيانات المتقدمة**: العلاقات بين الكيانات (مثلاً: مستخدم له عدة طلبات)، أنواع الحقول (صورة، ملف، تاريخ).
- **الصلاحيات**: أدوار (Admin, User, Manager)، تقييد الوصول للأوامر.

### ب. تطوير الـ ProgramContract
تحديث `formal_engine/schemas/program_contract.py` ليدعم:
- `logic_rules`: قواعد شرطية (إذا كان الرصيد > X اسمح بالطلب).
- `external_services`: تعريف الخدمات الخارجية المطلوبة.

---

## 2. معالجة النصوص الطويلة جداً (Extremely Long Texts)

### أ. استراتيجية "الخريطة الذهنية" (Mental Map Extraction)
بدلاً من إرسال النص الطويل مباشرة لترجمته إلى JSON، سنضيف مرحلة وسيطة في `spec_translator.py`:
1. **التحليل الأولي**: استخراج قائمة بالكيانات والأوامر الرئيسية فقط من النص الكامل.
2. **الترجمة التفصيلية**: تقسيم النص إلى أجزاء بناءً على الكيانات المستخرجة، وترجمة كل جزء بشكل منفصل.
3. **الدمج الحتمي**: دمج الـ JSONs الناتجة مع التأكد من عدم التكرار وتوافق العلاقات.

### ب. تحسين الـ SpecTranslator
- استخدام نماذج ذات نافذة سياق كبيرة (مثل `gpt-4o` أو `claude-3.5-sonnet`) بشكل افتراضي للنصوص الطويلة.
- تحسين الـ System Prompt ليتضمن تعليمات حول استخراج العلاقات المعقدة.

---

## 3. خطوات التنفيذ

1. **المرحلة 1**: تحديث `telegram_taxonomy.py` بالمفاهيم الجديدة.
2. **المرحلة 2**: تعديل `spec_translator.py` لإضافة دعم تقسيم النصوص (Chunking) والدمج.
3. **المرحلة 3**: تحديث `ProgramContract` و `Formal Engine` لاستيعاب المفاهيم المعقدة في التوليد.
4. **المرحلة 4**: اختبار النظام بطلب "بوت متجر عالمي متكامل" بنص طويل جداً.
